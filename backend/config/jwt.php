<?php
// +----------------------------------------------------------------------
// | JWT 配置
// +----------------------------------------------------------------------

return [
    // JWT 密钥
    // ============================================================
    // 重要安全提示：
    // 1. 生产环境 MUST 通过环境变量 JWT_SECRET 设置强随机密钥
    // 2. 密钥长度至少 32 字符，推荐使用 64 字符
    // 3. 密钥必须包含至少 2 种字符类型（大小写字母、数字、特殊字符）
    // 4. 使用 php generate_jwt_secret.php 生成强随机密钥
    // 5. 不同环境使用不同密钥，禁止共享密钥
    // ============================================================
    'secret' => env('JWT_SECRET', 'dev_default_secret_key_for_development_only_change_in_production'),
    // Token 有效期（分钟）
    'ttl' => env('JWT_TTL', 1440),
    // 刷新 Token 有效期（分钟）
    'refresh_ttl' => env('JWT_REFRESH_TTL', 10080),
    // 允许的加密算法
    'allowed_algs' => ['HS256'],
];
