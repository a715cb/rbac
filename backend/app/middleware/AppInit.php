<?php
// +----------------------------------------------------------------------
// | 应用初始化中间件
// +----------------------------------------------------------------------
namespace app\middleware;

use think\Request;

/**
 * 应用初始化中间件
 * @description 在请求处理流程的早期执行全局初始化操作。
 *              作为应用层面的基础设施中间件，适合放置一些全局性的初始化逻辑，
 *              如：全局变量设置、请求开始时间记录、环境检测、默认响应头设置等。
 * @设计意图:
 *   - 提供一个统一的入口点，在任何业务逻辑执行前执行初始化
 *   - 避免在多个控制器中重复初始化代码
 *   - 便于后续扩展全局性的前置处理逻辑
 * @初始化示例:
 *   - 记录请求开始时间，用于性能监控
 *   - 设置时区、语言环境
 *   - 初始化日志上下文
 *   - 检测是否为测试环境，启用调试模式
 *   - 设置 JSON 响应默认格式
 * @注意事项:
 *   - 保持初始化逻辑轻量，避免耗时操作影响响应速度
 *   - 初始化失败的降级处理需谨慎设计
 *   - 与其他中间件的执行顺序会影响初始化结果
 */
class AppInit
{
    /**
     * 处理请求（执行初始化）
     * @param Request $request 请求对象
     * @param \Closure $next 下一个中间件或控制器
     * @return mixed 后续中间件/控制器的返回值
     * @description 执行流程：
     *   1. 在请求进入业务逻辑前执行全局初始化操作
     *   2. 调用 $next($request) 将控制权交给下一个中间件或控制器
     *   3. 初始化逻辑在 $next 之前执行，确保在任何业务代码前生效
     * @优化建议:
     *   - 复杂初始化操作可考虑延迟加载或按需初始化
     *   - 初始化失败时应记录日志并考虑是否阻断请求
     */
    public function handle($request, \Closure $next)
    {
        // 应用初始化逻辑
        // 可以在这里执行一些全局的初始化操作

        // 继续处理请求
        return $next($request);
    }
}
