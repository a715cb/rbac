# 1. 项目概述

本项目是一个基于 **RBAC (Role-Based Access Control)** 模型的企业级权限管理系统，采用前后端分离架构，提供完整的用户管理、角色管理、菜单管理、部门管理、接口管理、字典管理及日志审计功能。

系统支持三级权限控制：**菜单权限**（页面可见性）、**按钮权限**（操作级别控制）、**接口权限**（API 级别控制），以及五级数据权限：全部数据、本部门数据、本部门及以下数据、仅本人数据、自定义数据。

**默认管理员账号**: `admin` / `123456`
